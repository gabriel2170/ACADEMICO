-o enum seria um comando de criação de uma estrutura ou objeto que podemos armazenar varias variaveis inicializando com determinado valor independente do tipo da variavel
-lembrando que vale a regra dos tipos de variaveis onde no caso apos inicializar as variaveis com valores ou tipos , depois nao tem como armazenar valores que nao condiz com o tipo da variavel

enum Item{
    nome = "laranja",
    tipo = "fruta",
    numero = "25",
}


let meuNome:Item = Item.nome


console.log(meuNome)
console.log(Item.nome)
console.log(Item.numero)
console.log(Item.tipo)



