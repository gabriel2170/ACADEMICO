-o click é um evento de quando nos clicamos no elemento
-colocar o last serve para selecionar o ultimo elemento 
-colocar o first serve para selecionar o primeiro elemento 
-o even serve para selecionar todos os elementos em posicoes impares da lista 
-o odd serve para selecionar todos os elementos em posicoes pares da lista 
-o eq(n°) serve para selecionar elementos de determinadas posicoes de um array
-o gt(n°) serve para selecionar elementos abaixo de outros elementos 
-o lt(n°) serve para selecionar elementos que estao antes de outros elementos 



$('.cars_list li:last').click(function(){
    console.log('cliquei no Ultimo')
    alert('OK')
    
})

$('.cars_list li:first').click(function(){
    console.log('cliquei no primeiro')
    alert('OK')
    
})

$('.cars_list li:even').css('background-color', "#ccc")

$('.cars_list li:odd').css('background-color', "#ccc")

$('.cars_list li:eq(1)').css('font-size', "40px")

$('.cars_list li:gt(2)').css('opacity', "0.3")

$('.cars_list li:lt(2)').css('opacity', "0.3")
