-Tipos de Seletores do JQuery


jQuery('body') - declaracao do JQuery

$('body') - declaracao do JQuery

$("#meu-id") - seletor pelo Id

$(".minha-classe") - seletor pela classe

$("#lista li.ativo") - seletor composto com id seguido de tag e classe

$('div.azul, li.red') - seletor composto com 2 classes seguido de 2 tags em cada

$(h1) - seletor pela Tag

$("imput[name=telefone]") - seletor por atributos 

$("tr:odd") - pseudo-seletor de condicao

$("li:visible") - pseudo-seletor para elemetos que estao visiveis apenas

$("div:gt(2)") - pseudo-seletor de posicao

$("posicao:checked") - pseudo-seletor de elementos que estao checados apenas

