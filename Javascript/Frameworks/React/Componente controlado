//input.jsx

-o target.value no parametro da funcao abaixo serve para selecionar o como alvo de leitura ou modificacao o valor armazenado nesse parametro
-e abaixo utilizamos o setV da tupla declarata [v, setV] para permitir atravez da função que o primeiro imput possa ser alterado no metodo onChange
-readOnly limita o imput para que voce apenas possa visualizar sem modificar
-undefined seria de valor indefinido , ou nada 



import React , {useState} from "react";
import './input.css'

export default props => {

    const [v , setV] = useState('Teste')
    function mudar(e){
        setV(e.target.value)
      
    }
    return (
        <div className='Input'>
            <h2>{v}</h2>
            <input value={v} onChange={mudar}/>
            <input value={v} readOnly/>
            <input value={undefined}/>
        </div>
    )
}

----------------------------------------------------------
//input.css

.Input input{
    font-size: 1.4rem;
}
