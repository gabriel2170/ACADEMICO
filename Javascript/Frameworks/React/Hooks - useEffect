
-o useEffect serve para reproduzir mudancas de estados no corpo da funcao do component de forma para evitar que a funcao gere infinitos callbacks da tentativa de mudanca de estado de um useState
-no useEffect vao 2 parametros, o primeiro seria a funcao ou algoritmo que vc vai utilizar para alterar um estado de uma variavel do useState ou qualquer outro elemento
-o segundo seria a dependencia que a funcao ou algoritmo precisa para poder ser executada 


import React, { useEffect, useState } from "react";

function calc(n) {
    if (n < 0) return -1
    if (n == 0) return 1

    return calc(n - 1) * n
}
function Hooks(props) {

    const [number, setNumber] = useState(0)
    const [fatorial, setFatorial] = useState(1)

    useEffect(function(){
        setFatorial(calc(number))
    }, [number])


    return (
        <div>
            <div>
                <span className="text"></span>
                <span className="text red">{fatorial}</span>
            </div>

            <input type="number" className="input"
                value={number}
                onChange={(e) => { setNumber(e.target.value) }}
            />
        </div>
    )
}



export default Hooks
