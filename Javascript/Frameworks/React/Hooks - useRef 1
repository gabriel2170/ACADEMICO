
-o useRef serve para atribuir uma referencia do valor para a variavel count 
-podemos usar o useEffect para atribuir uma nova referencia do valor a variavel count somente quando houver alteracao no valor da variavel valor1
-e o current é usado para acessar o valor atual da variavel do useRef

import React, { useState, useRef, useEffect } from "react";

const useref = (props) => {

    const [value1, setValue1] = useState("")
    const count = useRef(0)


    useEffect(()=>{
        count.current = count.current + 1
    })[value1]

    return (
        <div className="Useref">

            <div className="center">
                <div>
                    <span className="text">Valor: </span>
                    <span className="text">{value1}</span>
                    <span className="text">{count.current}</span>

                </div>
                <input type="text" className="input"
                    value={value1}
                    onChange={e => setValue1(e.target.value)}
                />
            </div>
        </div>
    )
}
