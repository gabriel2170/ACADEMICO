//index.js

-o AppRegistry registra tudo que estamos programando no App utilizando o comando registerComponent
-parecido com o comando render do reactDOM mas a diferenca sao os dois parametros passados
-o name ou appName como foi chamado que seria o nome do app importado do pacote json
-e o component App ou qualquer outro elemento que seria o conteudo que sera registrado no app


import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => App);

-----------------------------------------------------------------------------------
//app.js

-A tag Text serve para reproduzir textos no nosso App
-podemos reproduzir o HTML no nosso projeto por estarmos usando o metodo de jsx da biblioteca React
-sendo que podemos armazenar tags e elementos em variaveis 


import React from "react"
import { Text } from "react-native"

function App(){
  
  const x = <Text>Um text usando variaveis</Text>
  return (
  /*<Text >
    Texto de Teste
  </Text >
  */
    x
  )
}


export default App
