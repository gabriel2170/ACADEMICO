-crypto é um modulo de criptografia do node 
-o Date.now tambem serve para pegar a hora atual
-o metodo de cryptografia pdkdf2 serve para criar um Hash com determinada criptografia
-nesse algoritmo sera mostrado o tempo que a funcao demanda para criar um Hash , sendo que os 4 primeiros hashs sao criados simultaneamente , como em uma multithread
-mas repara que quando ele vai criar o quinto hash ele demora mais que os primeiros 4 , pois nao esta em uma multithread , e sim sendo criado em uma thread nova e unica, uma singlethread


const crypto = require('crypto')

const start = Date.now()



function logHashTime(){
    crypto.pbkdf2("a", "b", 100000, 512,"sha512", ()=>{
        console.log(`Hash: ${Date.now() - start}`)
    })
}

logHashTime()
logHashTime()
logHashTime()
logHashTime()
logHashTime()
