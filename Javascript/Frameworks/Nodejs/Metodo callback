
-o callback serviu para retornar o resultado da soma como um parametro da mesma funcao, de forma que o callback e chamado como parametro quando a funcao soma for chamada
-o setTimeout faz com que a funcao aguarde alguns segundos para a execução , e essa funcao vai 2 parametros, o que vai ser reproduzido ou retornado na funcao soma e o tempo que no caso foi 3000 milisegundos 
-o callback dessa funcao foi a outra funcao o resolveSoma() armazenada na variavel soma na funcao callback()
-e o dado armazenado para a funcao callback() na variavel x foi o 200
-throw serve para interromper a execução da funcao quando o valor err retorna true no if



function callback(x, soma){
    return setTimeout(()=>{
        return soma(null, x + 5000)
    },3000) 
    
}

function resolveSoma(err, data){
    if(err) throw err
    console.log(data)
}


callback(200, resolveSoma)

console.log(callback)
